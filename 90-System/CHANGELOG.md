# Cron Run Log — graphify incremental update

| Timestamp (UTC) | Result | Notes |
|---|---|---|
| 2026-07-26T21:30:00Z | partial | graphify update ran (1812 nodes / 3287 edges / 108 communities code slice); fix post-processor [noop] (already clean); mempalace incremental mine skipped — chromadb backend not installed in env |
| 2026-07-27T00:00:00Z | ok | graphify cluster-only applied to vault (50137 nodes / 97449 edges / 3908 communities); fix-community-links applied 2527 dead → 1118 anchors (1409 thin/skipped); mempalace mine skipped — same chromadb ModuleNotFoundError as 2026-07-26. Wrapper `graphify-with-fix.py` has a known .exe-suffix bug (calls `Scripts/graphify` w/o `.exe`; binary exists at `Scripts/graphify.exe`) — worked around by invoking the .exe directly. |
| 2026-07-28T14:35:00Z | partial | graphify cluster-only applied to vault (50137 nodes / 97449 edges / 3891 communities; report 313 KB); fix-community-links applied 2512 dead → 1123 anchors (1389 thin/unmatched). Wrapper binary-check bug recurred (logs `[err] graphify binary not found` then `[noop]`) — invoked `Scripts/graphify.exe` directly + ran fixer as separate step. Mempalace incremental mine (--limit 50) started but timed out after 300s on embedding persist (dry-run succeeded: 50 files / 7923 drawers mapped); the mempalace process left in-flight but cron scheduled to retry on next run. |
| 2026-07-29T19:45:00Z | partial | graphify cluster-only applied to vault (50137 nodes / 97449 edges / 3902 communities); fix-community-links applied 2521 dead → 1133 anchors (1388 thin/skipped). Same wrapper .exe-suffix bug — invoked `Scripts/graphify.exe` directly + fixer as separate step. Mempalace incremental mine skipped (known blocker): mp.sh hardcodes Python 3.14 → chromadb import crashes with `No module named 'numpy._core._multiarray_umath'`; pip install into 3.11 venv doesn't propagate to mp.sh's isolated env. Sweep not applicable (transcript-only). |

